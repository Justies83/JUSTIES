# 아침 브리핑 실행

한국시간 매일 06:00. 밤새 나온 소식을 정리해 글을 발행한다.

## 지침

`docs/daily-brief.md` 를 읽고 그대로 따른다. **그 문서가 이 작업의 유일한
기준이다.** 대상 섹션, 원문 확인 의무, 사실 / 해석 / 미확인 분리, 이미지
저작권 규칙, 도표 규격, 발행하지 않아야 할 조건이 모두 그 안에 있다.

이 파일에 규칙을 새로 적지 않는다. 규칙은 한 곳에만 있어야 한다.

## 이 저장소

`Justies83/JUSTIES` — "제의 / JUSTIES" 개인 아카이브, <https://justies.net>

작업 브랜치는 `claude/personal-editable-homepage-31hofg` 이다.
사이트가 배포되는 유일한 브랜치이므로 다른 곳에 커밋하면 글이 올라가지 않는다.

```bash
npm ci
npm run build
npx astro check
```

셋이 모두 통과한 뒤에만 커밋하고 push한다. 배포는 GitHub Actions가 하므로
Cloudflare 토큰은 필요하지 않다.

## 한 편씩 push한다

**다섯 편을 다 쓴 뒤에 몰아서 커밋하지 않는다.** 한 편을 완성할 때마다 빌드하고
커밋하고 push한 뒤 다음 편으로 넘어간다. 이 실행이 중간에 끝나도 그때까지 쓴 글은
사이트에 남아야 한다.

이 규칙을 지키지 않아 2026-08-25와 08-26 두 번의 실행이 각각 13분·17분 동안
작업하고도 커밋을 하나도 남기지 못했다.

실행을 끝내기 전에 확인한다.

```bash
git status --short     # 커밋 안 된 글이 남아 있으면 지금 커밋하고 push한다
git log origin/claude/personal-editable-homepage-31hofg --oneline -5
```

## 중복 피하기

`src/content/posts/` 를 먼저 훑어 최근 3일간 무엇을 다뤘는지 확인한다.
문체와 구성은 기존 글을 따른다.

## 보고

무엇을 발행했고 무엇을 버렸는지, 버린 이유를 함께 보고한다.
발행한 글은 커밋 해시와 함께 적는다.

항목 하나하나는 근거가 부실하면 버리는 것이 맞다. 다만 **하루 전체를 비우는 것은
다른 문제다** — 1번(미장 요약)은 증시 마감·환율처럼 매일 확인 가능한 재료가
있으므로, 아무것도 못 쓸 상황이면 최소 그 한 편은 쓴다. `docs/daily-brief.md` 의
"하루 최소 한 편" 을 따른다.

## 이메일 피드백

끝내기 전에 Gmail로 **justies@naver.com** 앞으로 짧은 메일을 보낸다.

- 제목: `[JUSTIES] 아침 브리핑 — YYYY-MM-DD`
- 본문: 발행한 글 제목·주소(`/posts/...`)를 항목당 한 줄씩, 버린 항목이
  있으면 그 이유도 한 줄씩. 위 "보고" 내용을 그대로 요약하면 된다.

메일 발송이 안 되면(연결 안 됨 등) 억지로 재시도하지 말고 그 사실만
보고에 남긴다. 메일을 못 보냈다고 글 발행·push를 미루지 않는다 — 순서는
항상 push가 먼저다.
