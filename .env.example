# Public base URL of the site. Copy to .env for local overrides.
SITE_URL=http://localhost:4321

# ── Blogspot 자동 발행 (scripts/blogspot/auto_post.py) ──────────────────────
# 로컬에서 돌릴 때만 쓴다. GitHub Actions 는 저장소 시크릿에서 읽는다.
# 설명: docs/routines/blogspot.md
# GEMINI_API_KEY=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REFRESH_TOKEN=
#
# POST_METHOD=mail 로 되돌릴 때만:
# SMTP_USER=
# SMTP_PASSWORD=
# BLOGGER_POST_EMAIL=계정이름.비밀단어@blogspot.com
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
