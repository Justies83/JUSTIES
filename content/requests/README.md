# 글 요청

`/admin` 의 **글 요청** 에서 주제를 넣으면 이 폴더에 파일이 하나 생긴다.
예약 실행이 이 폴더를 먼저 확인하고, `상태: 대기` 인 요청을 글로 만든다.

이 폴더는 사이트에 발행되지 않는다. Astro는 `src/content/posts/` 만 읽는다.
요청은 주문서이고, 결과물은 그쪽에 생긴다.

처리가 끝나면 예약 실행이 같은 파일의 `상태` 를 `완료` 로 바꾸고 `결과` 에 글 주소를
적는다. 그래서 요청 이력이 그대로 남고, 같은 요청이 두 번 처리되지 않는다.
